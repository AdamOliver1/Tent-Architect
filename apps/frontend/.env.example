# API base URL for backend requests.
# Leave empty to use relative /api (works with Vite proxy in dev, or reverse proxy in prod).
# Set to full URL (e.g. http://localhost:6000/api) for direct requests.
VITE_API_URL=

# Backend URL for Vite dev server proxy (only used when VITE_API_URL is empty).
# Change if your backend runs on a different host/port.
VITE_API_BACKEND=http://localhost:6000
