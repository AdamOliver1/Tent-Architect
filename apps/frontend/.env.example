# API base URL for backend requests.
# Leave empty to use relative /api (works with Vite proxy in dev).
# Production (Vercel): https://tent-architect-backend.vercel.app/api
VITE_API_URL=

# Backend URL for Vite dev server proxy (only used when VITE_API_URL is empty).
VITE_API_BACKEND=http://localhost:6000
