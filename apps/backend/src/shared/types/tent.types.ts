/**
 * Tent dimensions in meters
 */
export interface TentDimensions {
  /** Length of the tent in meters (Rail End direction) */
  length: number;
  /** Width of the tent in meters (Open End direction) */
  width: number;
}
